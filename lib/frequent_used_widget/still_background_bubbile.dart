import 'package:flutter/material.dart';

class CustomStillBackground extends StatelessWidget {
  const CustomStillBackground({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

import 'package:flutter/material.dart';

class DisplayCode extends StatelessWidget {
  const DisplayCode({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Display Widget'),),
    );
  }
}
